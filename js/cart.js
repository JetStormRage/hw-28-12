// Це мало б зробити щоб кнопочка + додавала 1 до значення кількості=(
function plus() {
   document.querySelector('.quantity')
    return ('.quantity') + 1;
}
const buttonPlus = document.querySelector('.button-plus')
buttonPlus.addEventListener('click', plus);


function sum() {
    Math.sum() '.quantity' * '.price';
}